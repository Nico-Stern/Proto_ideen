using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BP : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Vector2 Mitte= new Vector2(0,0);
        transform.position = Mitte; 
    }

    
}
